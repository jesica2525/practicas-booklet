let cantidad = parseInt(prompt("Ingrese la cantidad que debe repertirse un texto"))
const texto = "lado";

for (let i = 0; i < cantidad; i++) {
    if (cantidad > 4) {
        break;
    }
    alert(texto)
}